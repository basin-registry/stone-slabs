name = "plot"
version = "0.0.1"
entry = "main.stn"
description = "Data visualization: line, scatter, bar, area, heatmap, contour, surface plots"

deps = {
    display = "0.1.0"
    draw3d = "0.1.0"
}
